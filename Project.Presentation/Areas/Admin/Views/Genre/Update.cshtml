@using Project.Application.Models.DTOs.GenreDTOs

@{
    ViewData["Title"] = "Update";
}
@model UpdateGenreDTO

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Kategori Güncelle</h4>
        <form class="forms-sample" asp-action="Update" asp-controller="Genre" asp-area="Admin" method="post">
            <div class="form-group">
                <label asp-for="Name" class="form-label">Kategori Adı</label>
                <input asp-for="Name" type="text" class="form-control" required>
            </div>
            <div asp-validation-summary="All" class="bg-red text-white"></div>
            <button type="submit" class="btn btn-primary me-2">Güncelle</button>
        </form>
    </div>
</div>

